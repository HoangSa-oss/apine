
export const checkKeyword = async(idSession:string)=>{
    // const checkProcessing = await schemaurlpost.aggregate([
    //     {
    //         $match:{"idSession":idSession}
    //     },

    //     {
    //         $group:{
    //             _id:"$keyword"
    //         }
    //     }
    // ])
    // const checkSumKeyword = await schemakeyword.find({idSession:idSession}).select('lengthKeyword -_id')
    // const data = `${checkProcessing.length}/${checkSumKeyword[0].lengthKeyword}`
    // return data
}